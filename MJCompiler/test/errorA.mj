// errorA

program errorA

    int nula = 0, jedan =123, 2dva = 2, tri*, cetiri;
    int pet=5-;
    int sest[6];
 // int 123;
    int a, b, c;

    class Predmet extends extends A
    {
        int tezina;
        char vrsta = 5, #aslkd = 12, 123;
        char 123, a123;
        {
            void postaviTezinu(int tezina) {this.tezina = tezina;}  
        }
    }

    int 'sedam[7];
    const int osam=-8;
{
    int sumA(int 1z)
    {
        z = z + # + 1321 + 123;
        if( z > 0 && z >> 1 ) return -1;
        return z;
    }

    int sumB(int z, int2 d, int[] c, int d[])
    {
        sumA( 15, c, # );
        sumA( , );
        sumA( , , );
        return z +* d + e;
    }

    void main()	
    {}
}

// errorA
