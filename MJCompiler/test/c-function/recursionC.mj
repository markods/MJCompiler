// calculate the ackermann function
program recursionC
    int steps;
{
    int ack( int x, int y, bool init )
    {
        if( init ) { init = false; steps = 0; }
        steps++;

        if( x == 0 ) return y+1;
        if( y == 0 ) return ack( x-1, 1, false );
                     return ack( x-1, ack( x, y-1, false ), false );
    }

    void main()
        int i;
    {
        i = 0;
        do
        {
            print( ack( 4, i, true ), 2 ); print( ' ' );
            print( steps );                print( eol );
            i++;
        } while( i < 5 );

        return;
    }
}
